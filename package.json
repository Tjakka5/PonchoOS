{
  "name": "ownos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "nasm bootloader.asm -f bin -o bootloader.bin && nasm ExtendedProgram.asm -f bin -o ExtendedProgram.bin && copy /b bootloader.bin+ExtendedProgram.bin bootloader.flp",
    "start": "npm run build && bochsrc.bxrc"
  },
  "author": "",
  "license": "ISC"
}
